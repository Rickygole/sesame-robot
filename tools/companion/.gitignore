# Built by app/build_app.sh -- machine-specific absolute paths are baked
# into Info.plist's LSEnvironment, so this is never something to commit.
build/

# Created by `make app` via `uv venv --python 3.12`. Recreated on demand.
.venv/
